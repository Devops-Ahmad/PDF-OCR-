.venv/
.ocr_work/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Downloaded binaries (llama.cpp CUDA build for the Surya layout backend) --
# large, platform-specific, re-fetched by the setup step, never committed.
.tools/

# Secrets: cloud-fallback API keys must live here, never in config/*.yaml or code.
.env
*.key

# Benchmark output (scripts/synth_benchmark.py default --out)
synth_bench_out/
